'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('demo@foodsafe365.com');const [password,setPassword]=useState('demo');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const r=useRouter();async function login(){setBusy(true);setError('');try{const res=await fetch('/api/v1/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const body=await res.json();if(!res.ok)throw new Error(body?.error?.message||'Login failed');r.push('/home')}catch(e:any){setError(e.message||'Login failed')}finally{setBusy(false)}}return <main><div className="topbar"><div className="brand">FoodSafe365</div></div><div className="container" style={{maxWidth:520,paddingTop:72}}><div className="card"><h1>Welcome back</h1><p className="muted">Sign in to manage food safety.</p><div className="grid" style={{marginTop:24}}><label className="field"><span>Email or mobile</span><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className="field"><span>Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/></label>{error&&<p className="muted" role="alert">{error}</p>}<button className="btn primary" onClick={login} disabled={busy}>{busy?'Signing in…':'Log in'}</button><button className="btn secondary" type="button">Forgot password</button></div></div></div></main>}
