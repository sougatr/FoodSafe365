'use client'; import Link from 'next/link'; import {ArrowLeft,ArrowRight,CheckCircle2,Clock3,FileText,Home} from 'lucide-react'; import {sprint19 as d} from '@/lib/sprint19-demo';
export default function Documents(){return <main><div className="topbar"><Link href="/home" className="brand">FoodSafe365</Link><div style={{display:'flex',gap:10,alignItems:'center'}}><Link href="/home" className="btn secondary" style={{display:'inline-flex',alignItems:'center',gap:6,fontSize:13,padding:'6px 12px'}}><Home size={14}/> Home</Link><div className="muted">{d.outlet.name} · Manager</div></div></div><div className="container"><div className="breadcrumb"><Link href="/home">Home</Link> › <Link href="/manager"><ArrowLeft size={16}/> Manager overview</Link> › Documents</div><p className="eyebrow">DOCUMENTS & COMPLIANCE</p><h1>Keep important records current</h1><p className="lead">Track expiry and verification without mixing documents into daily operational checks.</p><div className="document-list">{d.documents.map(x=><div className="card document-row" key={x.title}><div className={`document-icon ${x.status}`}>{x.status==='valid'?<CheckCircle2/>:<Clock3/>}</div><div><h3>{x.title}</h3><p className="muted">{x.status==='valid'?'Current record':'Expires in '+x.due}</p></div><span className={`pill ${x.status==='valid'?'good':'attention'}`}>{x.status==='valid'?'Current':'Expiring soon'}</span><button className="btn secondary">Open <ArrowRight size={16}/></button></div>)}</div><div className="notice info"><FileText size={18}/><span>Document expiry is a management signal. FoodSafe365 does not infer legal validity beyond the document and verification data recorded.</span></div></div></main>}
