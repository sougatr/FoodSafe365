'use client'; import Link from 'next/link'; import {ArrowLeft,ArrowRight,CheckCircle2,AlertTriangle,Home,Wrench} from 'lucide-react'; import {sprint19 as d} from '@/lib/sprint19-demo';
export default function Equipment(){return <main><div className="topbar"><Link href="/home" className="brand">FoodSafe365</Link><div style={{display:'flex',gap:10,alignItems:'center'}}><Link href="/home" className="btn secondary" style={{display:'inline-flex',alignItems:'center',gap:6,fontSize:13,padding:'6px 12px'}}><Home size={14}/> Home</Link><div className="muted">{d.outlet.name} · Manager</div></div></div><div className="container"><div className="breadcrumb"><Link href="/home">Home</Link> › <Link href="/manager"><ArrowLeft size={16}/> Manager overview</Link> › Equipment</div><p className="eyebrow">EQUIPMENT HEALTH</p><h1>Equipment signals</h1><p className="lead">Connect equipment records with food-safety observations and maintenance decisions.</p><div className="equipment-list">{d.equipment.map(e=><div className="card equipment-row" key={e.id}><div className={`equipment-icon ${e.status}`}>{e.status==='good'?<CheckCircle2/>:e.status==='due'?<Wrench/>:<AlertTriangle/>}</div><div className="equipment-main"><div className="equipment-top"><h2>{e.name}</h2><span className={`pill ${e.status==='good'?'good':e.status==='due'?'attention':'action'}`}>{e.status}</span></div><p className="muted">{e.type} · {e.location}</p><p><strong>{e.signal}</strong></p><small className="muted">{e.calibration}</small></div><Link href={e.id==='ref2'?'/manager/risk/refrigeration':'/records'} className="btn secondary">Review <ArrowRight size={16}/></Link></div>)}</div></div></main>}
