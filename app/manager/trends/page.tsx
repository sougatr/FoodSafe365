'use client';
import Link from 'next/link'; import {ArrowLeft,ArrowUp,ArrowDown,Minus} from 'lucide-react'; import {sprint19 as d} from '@/lib/sprint19-demo';
export default function Trends(){return <main><div className="topbar"><div className="brand">FoodSafe365</div><div className="muted">{d.outlet.name} · Manager</div></div><div className="container"><div className="breadcrumb"><Link href="/manager"><ArrowLeft size={16}/> Manager overview</Link> › Trends</div><p className="eyebrow">FOOD SAFETY TRENDS</p><h1>What is improving?</h1><p className="lead">A simple view of recent control performance. Trends are management signals, not regulatory ratings.</p><div className="trend-range"><button className="tab active">30 days</button><button className="tab">7 days</button><button className="tab">90 days</button></div><div className="grid grid2 trend-grid">{d.trends.map(t=><div className="card trend-card" key={t.label}><div className="trend-top"><span className="eyebrow">CONTROL METRIC</span><span className={`trend-arrow ${t.trend==='down'?'bad':''}`}>{t.trend==='up'||t.trend==='up-good'?<ArrowUp/>:t.trend==='down'?<ArrowDown/>:<Minus/>}</span></div><h2>{t.value}%</h2><h3>{t.label}</h3><div className="progress"><span style={{width:`${t.value}%`}}/></div><p className="muted">Previous period: {t.baseline}%</p></div>)}</div><div className="card trend-note"><strong>What to look at first</strong><p className="muted">Temperature compliance and on-time corrective actions have moved down. Daily check completion is improving. Use the related records to understand why.</p><Link href="/manager/recommendations" className="btn primary">See recommendations</Link></div></div></main>}
