'use client';
import Link from 'next/link'; import {ArrowLeft,ArrowRight,CheckCircle2,Info,Lightbulb} from 'lucide-react'; import {sprint19 as d} from '@/lib/sprint19-demo';
export default function Recommendations(){return <main><div className="topbar"><div className="brand">FoodSafe365</div><div className="muted">{d.outlet.name} · Manager</div></div><div className="container"><div className="breadcrumb"><Link href="/manager"><ArrowLeft size={16}/> Manager overview</Link> › Recommendations</div><p className="eyebrow">FOODSAFE365 RECOMMENDATIONS</p><h1>What should you consider next?</h1><p className="lead">Recommendations are generated from recorded patterns and documents. A manager reviews and decides what to do.</p><div className="notice info"><Info size={18}/><span><strong>Human decision required.</strong> FoodSafe365 recommends; it does not automatically declare a food-safety violation or diagnose a cause.</span></div><div className="recommendation-list">{d.recommendations.map(r=><div className="card recommendation-row" key={r.id}><div className="recommendation-icon"><Lightbulb/></div><div className="recommendation-main"><div className="recommendation-meta"><span className="pill good">{r.type}</span><span className="muted">Confidence: {r.confidence}</span></div><h2>{r.title}</h2><p><strong>Why:</strong> {r.reason}</p><p className="muted"><strong>Suggested next step:</strong> {r.next}</p><div className="action-bar"><button className="btn primary"><CheckCircle2 size={17}/> Review recommendation</button><Link href={r.id==='rec-ref'?'/manager/risk/refrigeration':r.id==='rec-training'?'/manager/people':'/manager/documents'} className="btn secondary">View evidence <ArrowRight size={17}/></Link></div></div></div>)}</div></div></main>}
